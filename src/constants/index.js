//constants/index.js

const { ENUM_FRIEND_REQUEST_STATUS } = require("./FriendConstants");
const {
  ENUM_NOTIFICATION_TYPE,
  ENUM_ENTITY_TYPE,
} = require("./NotificationConstants");
const { ENUM_USER_PROVIDER } = require("./UserConstants");

module.exports = {
  ENUM_FRIEND_REQUEST_STATUS,
  ENUM_NOTIFICATION_TYPE,
  ENUM_ENTITY_TYPE,
  ENUM_USER_PROVIDER,
};
